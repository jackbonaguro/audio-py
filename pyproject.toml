[project]
name = "audio-py"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy==1.26.4",
    "pyaudio",
    "PySide6",
    "rubberband>=1.0.2",
]
