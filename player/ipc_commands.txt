# Main -> RT Commands

track_ready(shm_name: str, sample_len: int)  # GUI loads file, sends shared memory ref
play_track()
pause_track()
stop_track()
quit()
set_speed(speed: float) # 0.5 - 2.0
set_pitch(pitch: float) # -12 - 12

# RT -> Main Statuses
track_stopped()

# Main -> Main Statuses
load_progress(progress: float) # 0.0 - 1.0
load_status(
	status: "success" | "error",
	waveform: np.ndarray,
	duration: float,
	tempo: float
) # waveform shape: (4096, 2)
