# Main -> RT Commands

track_ready(shm_name: str, sample_len: int, track_id: int)  # GUI loads file, sends shared memory ref
play(track_id: int)
pause(track_id: int)
stop(track_id: int)
seek(position: float, track_id: int)
set_speed(speed: float, track_id: int)  # 0.5 - 2.0
set_pitch(pitch: float, track_id: int)  # -12 - 12
quit()  # no track_id

# RT -> Main Statuses
position(position: float, track_id?: int)
track_stopped(track_id?: int)

# Main -> Main Statuses
load_progress(progress: float)  # 0.0 - 1.0
load_status(
	status: "success" | "error",
	waveform: np.ndarray,
	duration: float,
	tempo: float,
	track_id?: int
)  # waveform shape: (4096, 2)
